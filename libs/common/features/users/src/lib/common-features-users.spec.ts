import { commonFeaturesUsers } from './common-features-users';

describe('commonFeaturesUsers', () => {
	it('should work', () => {
		expect(commonFeaturesUsers()).toEqual('common-features-users');
	});
});
